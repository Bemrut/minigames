"""import os

greating = (f"hello world number {19} again")

calculation_to_unit = 24 * 60
name_of_unit = "minutes"

x = input ("provide me days\n")
user_input = int(x)


def calc_units(days):
    print (f"{days} days has {days * calculation_to_unit} {name_of_unit}")
    print (greating)

calc_units(user_input)
print(os.name)"""
class User:
    
    def __init__(self):
        self.name
        self.surname
        # do smth